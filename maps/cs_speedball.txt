KILL KILL KILL KILL KILL KILL KILL KILL KILL KILL 
KILL KILL KILL KILL KILL KILL KILL KILL KILL KILL
KILL KILL KILL KILL KILL KILL KILL KILL KILL KILL
KILL KILL KILL KILL KILL KILL KILL KILL KILL KILL
KILL KILL KILL KILL KILL KILL KILL KILL KILL KILL 
KILL KILL KILL KILL KILL KILL KILL KILL KILL KILL 
KILL KILL KILL KILL KILL KILL KILL KILL KILL KILL 
KILL KILL KILL KILL KILL KILL KILL KILL KILL KILL

 
(Press FIRE to continue)
